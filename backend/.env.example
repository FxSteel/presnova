# Example .env for backend
# Copy to .env and fill sensitive values. Do NOT commit the real .env.

# Django
SECRET_KEY=change-me-for-production
DEBUG=True

# Bible API key
# Replace with your real API key in a local `backend/.env` file (do NOT commit)
# Example: BIBLE_API_KEY=your_api_key_here
BIBLE_API_KEY=your_api_key_here
